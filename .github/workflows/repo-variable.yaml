name: Step-Output_Demo
on: push
jobs:
  test-demo-kastro:
    runs-on: ubuntu-latest
    steps:
      - id: hello
        run: echo "msg=Hello GHA" >> $GITHUB_OUTPUT
      - run: echo "${{steps.hello.outputs.msg}}"
